{
  "name": "name",
  "description": "description",
  "author": "author",
  "scripts": {
    "start": "budo src/main.js:dist/build.js --live --port 3000",
    "build": "NODE_ENV=production browserify -p [ vueify/plugins/extract-css -o dist/build.css ] src/main.js | uglifyjs -c warnings=false -m > dist/build.js"
  },
  "browserify": {
    "transform": [
      "vueify"
    ]
  },
  "devDependencies": {
    "budo": "^8.3.0",
    "uglify-js-harmony": "^2.6.2",
    "vue": "^2.0.0-alpha.6",
    "vueify": "^9.2.0"
  }
}
