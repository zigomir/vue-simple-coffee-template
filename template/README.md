# {{ name }}

> {{ description }}

## Build Setup

``` bash
# install dependencies
npm install

# serve with live reload at localhost:3000
npm run dev

# build for production with minification
npm run build
```
